/* eslint-env mocha */

import { expect } from 'chai';
import React from 'react';

describe('<Search />', () => {
	it('should pass ', () => {
		expect(1 + 1 ===2).to.be.true;
	});
	xit('should pass ', () => {
		expect(1 * 1 ===3).to.be.true;
	});

	xit('should pass ', () => {
		expect(1 + 1 ===2).to.be.true;
	});
}); 


